{
  "_comment": "Copy this file to your plugin data directory as config.json to customise the timeline and the optional status-line clock. See README.md > Configuration for the exact path on your machine and what every field does. Every field is optional; anything you omit falls back to the built-in default shown here.",

  "timeFormat": "24h",
  "showSeconds": true,

  "labels": {
    "user": "auto",
    "assistant": "Claude"
  },

  "dateHeaders": true,
  "previewLength": 200,
  "includeToolCalls": false,

  "prefix": "⏱",
  "statuslineShowDate": true
}
